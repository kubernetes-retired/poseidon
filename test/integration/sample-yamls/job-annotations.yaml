apiVersion: batch/v1
kind: Job
metadata:
  name: jobtest-anno
spec:
  completions: 2
  template:
    metadata:
      annotations:
        "scheduler.alpha.kubernetes.io/name": poseidon-scheduler
      labels:
        app: jobtest-anno
    spec:
      containers:
      - name: jobtest-anno
        image: firmament/libhdfs3
        resources:
          requests:
            memory: "10Mi"
            cpu: "1000m"
          limits:
            memory: "1024Mi"
            cpu: "2000m"
        command: ["/bin/sh", "-c", "/cpu_spin 7"]
      restartPolicy: Never
